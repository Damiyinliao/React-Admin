import { useEffect } from "react";
// import { modules, pagePaths, mapMenusToRouter, components } from "@/router/routerHelper";
// import { useMenuStore } from "@/stores/menu";

const Role:React.FC = () => {

  // const { menuList } = useMenuStore();

  useEffect(() => {

    // console.log('menuList', menuList);
    // console.log('components', components);
    // console.log('mapMenusToRouter', mapMenusToRouter(menuList));

    // console.log('router', router);

  }, [])
  return (
    <div>
      {/* 角色管理{ files } */}
    </div>
  )
}
export default Role;